export const metadata = {
  title: "Next.js Authentication",
  description: "Authentication Without Library",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
